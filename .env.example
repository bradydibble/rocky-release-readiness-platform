# backend
PORT=8000
ADMIN_TOKEN=changeme
SECRET_KEY=changeme-use-a-real-secret-in-prod
DATABASE_URL=postgresql+asyncpg://r3p:changeme@localhost:5432/r3p

# frontend (nginx container)
FRONTEND_PORT=3000

# set to full URL in prod, leave as /api for local dev
VITE_API_BASE_URL=/api
